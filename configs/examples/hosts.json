{
  "local": {
    "name": "localhost_name",
    "certificates": [
      "local.json"
    ]
  },

  "remote": {
    "name": "srv1.example.com",
    "sync": true,
    "renew_on_sync": false,

    "cron": {
      "enabled": true,
      "user": "root",
      "frequency": "everyday @ 8h"
    },

    "certificates": [
      "example.com.json",
      "another.example.org.json"
    ],

    "ssh": {
      "host": "example.com",
      "port": 22222,
      "user": "root"
    },

    "reporter": {
      "enabled": true
    }

  }
}